# Minimal dependencies for Lambda functions in Step Functions and DynamoDB Streams
# These Lambda functions only perform AWS service operations (DynamoDB, CloudFormation, S3)
# They don't need ML libraries like Docling, PyTorch, or CUDA

# Core AWS SDK
boto3==1.37.0

# Boto3 type stubs (imported by app.routes.schemas, app.repositories.models)
boto3-stubs[bedrock,bedrock-agent-runtime,bedrock-runtime]==1.37.0

# For unique ID generation (used in app.repositories.common)
python-ulid==1.1.0

# For retry logic in update_bot_status.py
reretry==0.11.8

# For JWT token operations (imported by app.utils)
python-jose==3.4.0

# Database driver (imported by app.repositories)
pg8000==1.31.2

# For TypedDict in Python < 3.10
typing_extensions==4.13.1

# For schemas and data validation (used in app.routes.schemas)
pydantic==2.11.2

# For OpenSearch client (imported by app.repositories.common)
opensearch-py==2.4.2

# For AWS authentication with OpenSearch (imported by app.repositories.common)
requests-aws4auth==1.2.3

# For charset utilities (imported by app.routes.schemas.bot)
charset-normalizer==3.3.2

# For camelCase conversion (imported by app.routes.schemas.base)
pyhumps==3.8.0